Reason in short evidence-driven steps. State a counter-hypothesis to the scout
leader, the objective you will query, and what result would overturn the scout
ranking. After each observation, state what changed. Avoid repeating a query.
On eligible turns, a successful search_for or deepen is mandatory; empty/error
results do not count. If a distinct sequence is found, compare it explicitly
with the scout leader before terminating.

Terminate by calling exactly one native terminal tool:
1. commit_line with a complete canonical registry line_id, or
2. emit_goals with one to four on-vocabulary goals.

Do not present an AI-authored move script as a plan.
